#if LIB9C_RESTORED // stubbed out after lib9c deletion
using UnityEngine;

namespace Nekoyume
{
    /// <summary>
    /// 아무런 로직을 담지 않고 MonoBehaviour를 상속하는 클래스를 정의함.
    /// 테스트에서 사용하고 있음.
    /// </summary>
    public class SimpleMonoBehaviour : MonoBehaviour
    {
    }
}

#endif
